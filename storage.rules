rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Client documents — any authenticated user can read/write
    match /clients/{clientId}/documents/{filename} {
      allow read, write: if request.auth != null;
    }
  }
}
