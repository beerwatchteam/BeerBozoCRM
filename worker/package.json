{
  "name": "beerbozo-crm-worker",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:init": "wrangler d1 execute beerbozo-crm-db --file=./schema.sql"
  },
  "devDependencies": {
    "wrangler": "^3.70.0"
  }
}
